enum RequestType {
  get,
  getWithToken,
  post,
  postWithToken,
  postMultiPartWithToken,
  postMultiPart,
  putWithToken,
  put,
  patchWithToken,
  deleteWithToken,
}

enum UserRole { maker, checker }
